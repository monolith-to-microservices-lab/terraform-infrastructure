# Existing AWS resources (discovery)

> **Status: NÃO EXECUTADO.** Na execução de 2026-09-23 não havia AWS CLI instalado
> nem credenciais (`~/.aws` vazio, nenhuma variável `AWS_*`). A conta ainda não foi
> inspecionada.
>
> Antes do primeiro apply:
> ```powershell
> ./scripts/discover-aws.ps1 -Region <região>
> ```
> O script sobrescreve este arquivo com a tabela real.

| RESOURCE | CURRENT AWS ID | MANAGED BY TERRAFORM? | ACTION |
|---|---|---|---|
| (pendente) | – | – | – |
